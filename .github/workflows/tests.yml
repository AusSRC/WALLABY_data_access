name: Tests
on: [push]
jobs:
  tests:
    runs-on: ubuntu-latest
    env:
      DJANGO_ALLOW_ASYNC_UNSAFE: True
      DJANGO_SETTINGS_MODULE: "orm.settings"
      DJANGO_SECRET_KEY: ${{ secrets.DJANGO_SECRET_KEY }}
      DATABASE_NAME: wallabydb
      DATABASE_HOST: ${{ secrets.DATABASE_HOST }}
      DATABASE_USER: ${{ secrets.DATABASE_USER }}
      DATABASE_PASS: ${{ secrets.DATABASE_PASS }}
      WALLABY_DATABASE_PATH: './WALLABY_database'
      PYTHONPATH: .
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.8.0'
          architecture: 'x64'
      - name: Cache pip
        uses: actions/cache@v2
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-build-${{ env.cache-name }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}
      - run: git clone https://github.com/AusSRC/WALLABY_database.git
      - run: echo "PYTHONPATH=$PYTHONPATH:WALLABY_data_access" >> $GITHUB_ENV
      - run: echo "PYTHONPATH=$PYTHONPATH:." >> $GITHUB_ENV
      - run: pip3 install -r src/requirements.txt
      - run: pip3 install -r tests/requirements.txt
      - run: pytest -vs tests
